# SoundFonts

Direct SF2 playback looks for `/soundfonts/default.sf2` by default.

Add a licensed `.sf2` file named `default.sf2` here, or choose a local `.sf2` file from the playback bar in the app. SoundFont files can be large, so this project does not include a bundled default bank.
