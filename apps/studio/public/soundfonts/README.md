# SoundFonts

Direct SF2 playback uses `/soundfonts/default.sf2` by default.

The bundled default bank is GeneralUser GS v1.471. Users can still choose another local `.sf2` file from the playback bar.

Confirm redistribution terms before publishing builds that contain third-party SoundFont assets.
