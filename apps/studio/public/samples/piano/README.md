# Piano Samples

This folder contains small generated placeholder WAV samples so browser sample playback works in a static build.

Replace these files with licensed extracted SoundFont samples for higher-quality playback. Keep the note names in `sample-map.json`, or update the map to point at your own `.mp3`, `.ogg`, or `.wav` files.
